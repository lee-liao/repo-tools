# Dependencies
node_modules/

# Build output (intermediate files)
dist/

# VSCode extension build artifacts
vscode-extension/node_modules/
vscode-extension/out/
vscode-extension/*.vsix
vscode-extension/package-lock.json

# Internal documentation (not for public)
RECOVERY_COMPLETE.md

# Keep deployment artifacts in deploy folder
# deploy/ folder is committed to track deployment-ready files